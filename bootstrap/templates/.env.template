# ═══════════════════════════════════════════════════════════════════════════
# OpenClaw Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════
#
# Copy this file to ~/.openclaw/workspace/.env and configure your API keys.
# Run 'openclaw-setup' for an interactive setup wizard.
# Run 'openclaw-validate' to verify your configuration.
#
# SECURITY: This file contains sensitive credentials. Never commit to git.
# Permissions should be 0600 (owner read/write only).
#
# ═══════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────
# AI/LLM API Keys (Core functionality)
# ───────────────────────────────────────────────────────────────────────────

# Anthropic (Claude)
# Get your key: https://console.anthropic.com/
# Format: sk-ant-api03-...
ANTHROPIC_API_KEY=

# OpenAI (GPT-4, GPT-3.5)
# Get your key: https://platform.openai.com/api-keys
# Format: sk-proj-... or sk-...
OPENAI_API_KEY=

# Google (Gemini)
# Get your key: https://aistudio.google.com/apikey
GOOGLE_API_KEY=

# ───────────────────────────────────────────────────────────────────────────
# GitHub Integration
# ───────────────────────────────────────────────────────────────────────────

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
# Format: ghp_... (classic) or github_pat_... (fine-grained)
GITHUB_PAT=

# ───────────────────────────────────────────────────────────────────────────
# Productivity Integrations (MCP Servers)
# ───────────────────────────────────────────────────────────────────────────

# Todoist - Task Management
# Get token: https://todoist.com/prefs/integrations (scroll to API token)
TODOIST_API_TOKEN=

# Slack - Team Messaging
# Setup guide: ~/.openclaw/productivity/slack-setup.md
# Create app: https://api.slack.com/apps
# Bot Token format: xoxb-...
SLACK_BOT_TOKEN=
# App Token format: xapp-... (optional, for Socket Mode)
SLACK_APP_TOKEN=

# Brave Search - Web Search MCP
# Get key: https://brave.com/search/api/
BRAVE_API_KEY=

# ───────────────────────────────────────────────────────────────────────────
# Email Configuration (Email MCP Server)
# ───────────────────────────────────────────────────────────────────────────

# IMAP settings (for reading emails)
EMAIL_IMAP_HOST=imap.gmail.com
EMAIL_IMAP_PORT=993

# SMTP settings (for sending emails)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587

# Email credentials
# For Gmail: Use an App Password, not your regular password
# Create at: https://myaccount.google.com/apppasswords
EMAIL_USERNAME=
EMAIL_PASSWORD=

# ───────────────────────────────────────────────────────────────────────────
# Database & Storage (Auto-configured)
# ───────────────────────────────────────────────────────────────────────────

# Local SQLite database for memory system
DATABASE_PATH=data/memory.db

# Memory system directories
MEMORY_DIR=memory
LOGS_DIR=memory/logs

# Embedding model for semantic search
EMBEDDING_MODEL=text-embedding-ada-002

# ───────────────────────────────────────────────────────────────────────────
# Supabase (Optional - for cloud database)
# ───────────────────────────────────────────────────────────────────────────

# Get from: Supabase Dashboard > Project Settings > Database
# Format: postgresql://postgres:[password]@[host]:[port]/postgres
# SUPABASE_DB_URL=

# ───────────────────────────────────────────────────────────────────────────
# Dropbox (Optional)
# ───────────────────────────────────────────────────────────────────────────

# Dropbox API access token
# Create app: https://www.dropbox.com/developers/apps
# DROPBOX_ACCESS_TOKEN=

# ═══════════════════════════════════════════════════════════════════════════
# Quick Reference
# ═══════════════════════════════════════════════════════════════════════════
#
# Setup Commands:
#   openclaw-setup      Interactive credential configuration
#   openclaw-auth       Authenticate CLI tools and OAuth services
#   openclaw-validate   Verify all services are working
#
# Google Services (Calendar/Drive):
#   These require OAuth credentials (JSON file), not API keys.
#   Run: openclaw-setup --google
#   Or see: ~/.openclaw/productivity/google-calendar-setup.md
#
# ═══════════════════════════════════════════════════════════════════════════
